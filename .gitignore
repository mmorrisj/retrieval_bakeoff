.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Cached embeddings and downloaded corpora. Results themselves are committed;
# the multi-gigabyte inputs that produced them are not.
#
# Anchored to the repository root on purpose: an unanchored `data/` also
# matches src/bakeoff/data/, which holds the bundled smoke corpus the package
# needs to run at all.
.cache/
/data/

.env
.DS_Store
